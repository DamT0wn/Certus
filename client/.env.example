# Optional public API prefix. Never put secrets in VITE_ variables.
VITE_API_BASE_URL=
# Self-contained browser mock is enabled by default. Set false only when running the optional backend.
VITE_USE_BROWSER_MOCK=true
# Development proxy target, server-side only. Default: http://localhost:5000
API_PROXY_TARGET=
